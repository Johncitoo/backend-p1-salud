# Nestjs specific
/dist
/node_modules
/build
/tmp

# Documentos adjuntos cifrados guardados en disco (STORAGE_PROVIDER=LOCAL)
/storage

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# dotenv environment variable files (cualquier .env*, incluye .env.grupo12,
# .env.local, etc. — antes solo se ignoraban 4 nombres exactos y quedaban
# afuera variantes como esa, que terminaron committeadas con credenciales reales)
.env*
!.env.example

# temp directory
.temp
.tmp
